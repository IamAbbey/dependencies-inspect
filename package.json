{
  "name": "dependencies-inspect",
  "license": "MIT",
  "scripts": {
    "webui:install": "cd src/webui && npm install",
    "webui:ci-lint": "cd src/webui && npm run ci-lint",
    "webui:type-check": "cd src/webui && npm run type-check",
    "webui:build": "cd src/webui && npm run build",
    "yarn_plugin:install": "cd src/yarn_plugin && yarn install",
    "yarn_plugin:ci-lint": "cd src/yarn_plugin && yarn run ci-lint",
    "yarn_plugin:test": "cd src/yarn_plugin && yarn run test",
    "yarn_plugin:build": "cd src/yarn_plugin && yarn run build"
  }
}
